@{
  Layout = "_Layout";
}
@using HairSalon.Models;
@model HairSalon.Models.Client;
<div class="sidebar"></div>

<h2>Client Details:</h2>
  <hr />
@* @if(@Model.Stylists.Count == null)
{
  <h3>This Client Has No Assigned Stylist.</h3>
}
else
{
  @foreach(Client client in Model.Stylists)
  { *@
  <div class="sidebar">
  <p>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</p>

  <h3>@Html.DisplayNameFor(model => model.Mobile): @Html.DisplayFor(model => model.Mobile)</h3>

  <p>@Html.ActionLink("Back to list", "Index")</p>
  <p>@Html.ActionLink("Edit Client", "Edit", new { id = Model.ClientId })</p>
  <p>@Html.ActionLink("Delete Client", "Delete", new { id = Model.ClientId })</p>
</div>
  @* } *@
}

  
  